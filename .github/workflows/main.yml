name: Automated Market Scanner

on:
  workflow_dispatch:
  schedule:
    # Morning Scan at 9:15 AM IST (03:45 UTC), Mon-Fri
    - cron: '45 3 * * 1-5'
    # Pre-Close Scan at 3:10 PM IST (09:40 UTC), Mon-Fri
    - cron: '40 9 * * 1-5'

jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # <--- FIXES EXIT CODE 128 (ALLOWS GIT PUSH)
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install yfinance pandas numpy requests
      - name: Run Market Scanner
        run: python run_scan.py
      - name: Commit & Push Updated Report
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add breakoutsummary.md
          git commit -m "Auto-update breakoutsummary.md report" || exit 0
          git push
